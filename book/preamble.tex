% ============================================================
% Introduction to Image Processing with Python
% Shared LaTeX preamble
% Compile with LuaLaTeX + Biber (latexmk recommended).
% ============================================================

% ------------------------------------------------------------
% Book metadata
% ------------------------------------------------------------
\newcommand{\booktitle}{Introduction to Image Processing with Python}
\newcommand{\booksubtitle}{Algorithms, Implementation and Practical Image Processing}
\newcommand{\bookauthor}{Robert Elliott BSc (Hons), MBCS}

% ------------------------------------------------------------
% Typography and language
% ------------------------------------------------------------
\usepackage{fontspec}
\usepackage[british]{babel}
\usepackage{csquotes}
\usepackage{microtype}

% Keep the default LuaLaTeX fonts during drafting. Publication fonts and
% final trim size should be chosen once the target print platform is known.

% ------------------------------------------------------------
% Page layout -- drafting size
% ------------------------------------------------------------
\usepackage[
    a4paper,
    inner=30mm,
    outer=25mm,
    top=25mm,
    bottom=30mm
]{geometry}

% ------------------------------------------------------------
% Running heads and page numbers
% ------------------------------------------------------------
\usepackage[automark]{scrlayer-scrpage}
\clearpairofpagestyles
\ihead{\headmark}
\ohead{\pagemark}
\automark[section]{chapter}

% ------------------------------------------------------------
% Mathematics
% ------------------------------------------------------------
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{siunitx}

% ------------------------------------------------------------
% Graphics
% ------------------------------------------------------------
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{tikz}

% ------------------------------------------------------------
% Tables
% ------------------------------------------------------------
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{array}

% ------------------------------------------------------------
% Lists
% ------------------------------------------------------------
\usepackage{enumitem}

% ------------------------------------------------------------
% Colours
% ------------------------------------------------------------
\usepackage{xcolor}

\definecolor{CodeBackground}{RGB}{248,248,248}
\definecolor{CodeFrame}{RGB}{210,210,210}

% ------------------------------------------------------------
% Source code -- minted
% ------------------------------------------------------------

\usepackage[newfloat]{minted}

\setminted{
    autogobble=true,
    bgcolor=CodeBackground,
    breaklines=true,
    fontsize=\small,
    frame=single,
    framesep=2mm,
    linenos=true,
    numbersep=8pt,
    rulecolor=\color{CodeFrame},
    tabsize=4
}

% Python-specific options
\setminted[python]{
    tabsize=4
}

% C-specific options
\setminted[c]{
    tabsize=4
}

% Terminal commands
\setminted[bash]{
    linenos=false
}

% YAML
\setminted[yaml]{
    linenos=false
}

% ------------------------------------------------------------
% Reusable boxes
% ------------------------------------------------------------
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}

\newtcolorbox{historicalnote}{
    title=Historical note,
    breakable,
    colback=black!2,
    colframe=black!35
}

\newtcolorbox{implementationnote}{
    title=Implementation note,
    breakable,
    colback=black!2,
    colframe=black!35
}

\newtcolorbox{portfolio}{
    title=Original portfolio/sign-off item,
    breakable,
    colback=black!2,
    colframe=black!35
}

\newtcolorbox{projectfiles}{
    title=Project files,
    breakable,
    colback=black!2,
    colframe=black!35
}

\newtcolorbox{definitionbox}{
    title=Definition,
    breakable,
    colback=black!2,
    colframe=black!35
}

% ------------------------------------------------------------
% References
% ------------------------------------------------------------
\usepackage[
    backend=biber,
    style=ieee,
    sorting=none,
    giveninits=true
]{biblatex}
\addbibresource{references.bib}

% ------------------------------------------------------------
% Captions and cross-references
% ------------------------------------------------------------
\usepackage{caption}
\usepackage{subcaption}
\usepackage{hyperref}
\usepackage[nameinlink,noabbrev]{cleveref}

\hypersetup{
    pdftitle={\booktitle},
    pdfauthor={Robert Elliott},
    hidelinks
}

% ------------------------------------------------------------
% Index support
% ------------------------------------------------------------
\usepackage{makeidx}
\makeindex

% ------------------------------------------------------------
% Convenience commands
% ------------------------------------------------------------
\newcommand{\pyimage}{\texttt{pyimage}}
\newcommand{\python}{Python}
\newcommand{\numpy}{NumPy}
\newcommand{\opencv}{OpenCV}
\newcommand{\scikitimage}{scikit-image}
\newcommand{\historicalstars}[1]{\texttt{#1}}
